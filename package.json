{
  "name": "clocktimetask",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev":      "node build.mjs --dev",
    "build":    "node build.mjs",
    "lint":     "eslint src/",
    "lint:fix": "eslint src/ --fix"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "eslint":  "^9.0.0"
  }
}
